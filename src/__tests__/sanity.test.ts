// Feature: exam-modes — Vitest infrastructure sanity check

import { describe, expect, it } from 'vitest';

describe('vitest sanity check', () => {
  it('runs basic arithmetic', () => {
    expect(1 + 1).toBe(2);
  });
});
